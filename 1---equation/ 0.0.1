#include <stdio.h>
#include <conio.h>
typedef double dl;
dl desk(dl a, dl b, dl c)
	{
	return b*b - 4 * a*c;
	}
void resh(dl a, dl b, dl c)
{
	dl x1 = 0, x2 = 0;
	if (a != 0)
	{
		if (desk(a, b, c) >= 0)
		{
			if (desk(a, b, c) == 0)
			{
				printf("Odin koren: \n x = %g \n", -b / (2 * a));
			}
			else
			{
				x1 = (-b + desk(a, b, c)) / (2 * a);
				x2 = (-b - desk(a, b, c)) / (2 * a);
				printf("Dva kornya: \n x = %g \n x = %g \n", x1, x2);
			}
		}
		else
			printf("Kornei net");

	}
	else
	{
		if (b != 0)
			printf("Odin koren: \n x = %g \n", -c/b);
		else
		{
			if (c == 0)
				printf("Kornei beskonechnoe mnozhestvo");
			else
			printf("Kornei net");
		}
	}

}
int main()
	{
	dl a = 0, b = 0, c = 0, x1 = 0, x2 = 0;
	printf("Vvedite a,b,c cherez probel: \n");
	scanf("%lg %lg %lg", &a, &b, &c);
	resh(a, b, c);


	_getch();
	return 0;
	}
